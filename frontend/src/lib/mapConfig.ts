import type { LatLngTuple } from 'leaflet'

// Same city center as the Phase 2 seed data — easy to change for a different demo city.
export const DEFAULT_CENTER: LatLngTuple = [39.7392, -104.9903]
export const DEFAULT_ZOOM = 11 // city-level, not street-level
